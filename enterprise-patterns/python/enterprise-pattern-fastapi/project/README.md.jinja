# {{ service_name }}

{{ service_description }}

## Getting Started

### Prerequisites

- Python 3.12+
- [uv](https://docs.astral.sh/uv/) (`brew install uv` or `pip install uv`)
- Bazel (see `MODULE.bazel` at the monorepo root)
{% if use_database %}
- PostgreSQL — provisioned automatically via CloudNativePG in the local k3d
  cluster. Run `cd monorepo && tilt up` to start. The Tiltfile deploys the
  CNPG operator and creates a local Postgres cluster with a database for this
  service. Port 5432 is forwarded to localhost for psql/pgAdmin access.
{% endif %}

### Setup

```bash
# Install dependencies and create virtual environment
uv sync --dev

# Run the service
uv run uvicorn {{ module_name }}.main:app --host 0.0.0.0 --port {{ service_port }} --reload

# Run tests
uv run pytest
```

### Building with Bazel

```bash
# Build the service
bazel build //services/{{ service_name }}/...

# Run all tests
bazel test //services/{{ service_name }}/...
```

### Building with Docker

```bash
# Build the base image first (if not already built)
docker build -f monorepo/services/Dockerfile.base -t tdp-python-base monorepo/services/

# Build the service image (from monorepo root)
docker build -f services/{{ service_name }}/Dockerfile -t {{ service_name }} .
```

## Project Structure

```
src/{{ module_name }}/
  __init__.py          # Package exports
  main.py              # FastAPI app factory
  config.py            # Pydantic settings
  schemas.py           # Pydantic models
  exceptions.py        # Exception hierarchy
  dependencies.py      # FastAPI dependency injection
  telemetry.py         # OpenTelemetry setup
  routers/
    health.py          # /health and /version endpoints
{% if use_database %}
  database.py          # Async SQLAlchemy engine & session
  models/              # SQLAlchemy ORM models
{% endif %}
tests/
  conftest.py          # Shared fixtures
  test_health.py       # Health endpoint tests
  test_schemas.py      # Schema round-trip tests
  test_properties.py   # Property-based tests
pyproject.toml         # Dependencies and project metadata
uv.lock                # Pinned lockfile (committed)
```

## Dependency Management

This project uses [uv](https://docs.astral.sh/uv/) for package management.

```bash
# Add a dependency
uv add somepackage

# Add a dev dependency
uv add --dev somepackage

# Update lockfile after editing pyproject.toml
uv lock

# Install/sync to match lockfile
uv sync --dev
```

## Maintaining BUILD Files

The initial `BUILD.bazel` is generated by the Copier template as a starting point.
After generation, BUILD files are maintained by
[Gazelle](https://github.com/bazelbuild/rules_python/tree/main/gazelle)
(the `rules_python` Gazelle extension). Run Gazelle whenever dependencies change:

```bash
bazel run //:gazelle
```

Do not manually edit `BUILD.bazel` for dependency changes -- let Gazelle manage them.

## Post-Generation Checklist

After generating this service from the template, complete these steps:

- [ ] Create Helm chart at `deploy/charts/{{ service_name }}/` (copy from `deploy/charts/common/` and customize)
- [ ] Register in `release-please-config.json` and `.release-please-manifest.json`
- [ ] Add service to Tiltfile for local development
- [ ] Run `bazel run //:gazelle` to sync BUILD files with actual dependencies
{% if use_database %}
- [ ] Create the database and run initial Alembic migration
- [ ] Add `migration-job.yaml` to the Helm chart (gated on `migrations.enabled` value)
- [ ] Add database to the CNPG Cluster CRD in `deploy/postgres/cluster-local.yaml`

### Migration Strategy

- **Local:** `entrypoint.sh` runs `alembic upgrade head` before starting the app
  (controlled by `RUN_MIGRATIONS_ON_START` env var, defaults to `true`).
- **Dev/Production:** A K8s Job (Helm pre-upgrade hook) runs migrations before
  the Deployment rolls out. Set `RUN_MIGRATIONS_ON_START=false` in Helm values.
- **Backward compatibility rule:** All migrations must be backward-compatible with
  the previous application version. Destructive changes (column drops, renames) are
  split across two releases: Release N deploys new code, Release N+1 drops the column.
{% endif %}

[project]
name = "tdp-fastapi-core"
version = "0.1.0"
description = "Core FastAPI library for The Data Platform services"
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.backends"

{
  "name": "tdp-cluster",
  "version": "0.0.1",
  "description": "Kubernetes cluster provisioning for The Data Platform",
  "main": "index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@pulumi/pulumi": "^3.0.0",
    "@pulumi/command": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}

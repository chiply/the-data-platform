apiVersion: v2
name: schema-registry
description: Helm chart for the schema-registry service
type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
  - name: common
    version: 0.1.0
    repository: file://../common

# Production environment values for schema-registry.

image:
  repository: ghcr.io/chiply/the-data-platform/schema-registry
  pullPolicy: IfNotPresent
  # tag is set per-release, do not default to latest

imagePullSecrets:
  - name: ghcr-credentials

replicaCount: 2

migrations:
  enabled: true

env:
  - name: RUN_MIGRATIONS_ON_START
    value: "false"
  - name: DB_HOST
    valueFrom:
      secretKeyRef:
        name: tdp-db-credentials
        key: host
  - name: DB_PORT
    valueFrom:
      secretKeyRef:
        name: tdp-db-credentials
        key: port
  - name: DB_NAME
    valueFrom:
      secretKeyRef:
        name: tdp-db-credentials
        key: database
  - name: DB_USER
    valueFrom:
      secretKeyRef:
        name: tdp-db-credentials
        key: username
  - name: DB_PASSWORD
    valueFrom:
      secretKeyRef:
        name: tdp-db-credentials
        key: password
  - name: DB_SSLMODE
    value: "require"

podDisruptionBudget:
  enabled: true
  minAvailable: 1

resources:
  limits:
    cpu: 500m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi

ingress:
  enabled: true
  className: ""
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
  hosts:
    - host: schema-registry.prod.example.com
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: schema-registry-tls
      hosts:
        - schema-registry.prod.example.com
